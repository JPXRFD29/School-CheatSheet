<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Template.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Template.Pages"
    xmlns:models="using:Template.Data.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#e6e6e6">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15*" x:Name="LeftColumn"/>
            <ColumnDefinition Width="70*" x:Name="ContentColumn"/>
            <ColumnDefinition Width="15*" x:Name="RightColumn"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="1" Background="#F5F5F5">

            <ListView ItemsSource="{x:Bind Movies}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Movie">

                        <Border Width="200"
                                Height="350"
                                Background="White"
                                CornerRadius="4"
                                Padding="0"
                                BorderThickness="1"
                                BorderBrush="#DDDDDD"
                                Margin="4">

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Hardcoded image for all items -->
                                <Image Source="/Resource/Movies/Posters/MarioGalaxyMovie.jpeg"
                                       VerticalAlignment="Top"/>

                                <StackPanel Grid.Row="1"
                                            Padding="12 4"
                                            VerticalAlignment="Top">

                                    <Viewbox Height="22" HorizontalAlignment="Left">
                                        <RatingControl MaxRating="5"
                                                       Value="{Binding Rating}"
                                                       HorizontalAlignment="Left"/>
                                    </Viewbox>

                                    <TextBlock Text="{Binding Name}"
                                               FontSize="14"
                                               Margin="0,0,0,12"
                                               TextWrapping="Wrap"/>

                                    <Button Content="Buy Ticket"
                                            HorizontalAlignment="Stretch"
                                            Background="#3a7ff9"
                                            Foreground="White"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                    </DataTemplate>
                </ListView.ItemTemplate>

                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
            </ListView>
        </Grid>
    </Grid>
</Page>
